---
sidebar_position: 1
---

# id

### Overview

Query user identity information (uid/gid/groups) of the current or a specified user (read-only).

### Command Parameters

No parameters

### Usage Example

```bash
acli system id
```

### Output Example

```bash
# acli system id
uid=0(root) gid=0(root) groups=0(root)
```
