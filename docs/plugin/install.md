---
sidebar_position: 10
---

# install
操作概述: 安装指定插件

命令参数:
```bash
--path, -p  string  必要参数，插件路径，必须是 /support/tmp 目录下的插件，示例：/support/tmp/demo.aclix
```

使用示例:
```bash
acli plugin install --path /support/tmp/demo.aclix
```

结果示例:
```bash
插件安装成功