---
sidebar_position: 1
---

# id

### 操作概述

查询当前用户或指定用户的身份信息（uid/gid/groups，只读）。

### 命令参数

无参数

### 使用示例

```bash
acli system id
```

### 结果示例

```bash
# acli system id
uid=0(root) gid=0(root) groups=0(root)
```
